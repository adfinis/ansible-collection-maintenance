---

- name: Install packages
  ansible.builtin.apt:
    name:
      - openssh-server
      - postfix

- name: SSH working directory
  ansible.builtin.file:
    path: /run/sshd
    state: directory
    mode: "0755"
    owner: root
    group: root
